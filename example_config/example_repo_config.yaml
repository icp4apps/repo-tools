# Sample configuration for CP4A application stack CRD creation
name: CP4A stack CRDs
description: A sample configuration file to generate stack CRDs for the default application stacks.
version: 0.0.1
stack_groups:
  - name: default
    repos:
      - url: https://github.com/odo-devfiles/registry/blob/master/devfiles/index.json
        exclude: 
          - nodejs
          - openLiberty
          - quarkus
          - springboot
